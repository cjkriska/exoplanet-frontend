function Navbar() {
    return (
        <nav className="relative flex h-16 items-center justify-between">
            <div>ExoPlanets</div>
            <button>Login</button>
        </nav>
    );
}

export default Navbar;